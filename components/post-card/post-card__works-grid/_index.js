// Post Card Works Grid component scripts
// Can be extended with additional interactive features if needed

const initPostCardWorksGrid = () => {
    // Any additional interactive features for individual cards can go here
};

initPostCardWorksGrid();
